services:
  app:
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
    volumes: [".:/var/www/html"]
    profiles: ["dev"]

  node:
    command: >
      sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports: ["5173:5173"]
    profiles: ["dev"]
